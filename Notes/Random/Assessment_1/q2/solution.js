function makeNegative(num) {
  //if the number is less than 0 it will return it as we want only want negative numbers to come back 
    if(num < 0 ){
        return num;
        //else if a number is positive it will make it negative then return it.
    } else {
      return num * -1
    }
}