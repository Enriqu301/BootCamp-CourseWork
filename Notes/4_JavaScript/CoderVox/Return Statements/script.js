function emotion (feelings) {
    return feelings;
}

var currentFeelings = emotion("really love");

function theNumber (number) {
    return number; 
}

var times = theNumber(3);

function rest(place1) {
    console.log("I" + currentFeelings + "eating at " + place1 + "and ive been there" + times + " times")
}

rest("mc"); 